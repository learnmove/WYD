function buscar_noticia(url, value) {	window.location = url+value;}function buscar_noticia_bt(url, value, e) {	if (e.keyCode) // testa se é IE		var tecla = e.keyCode; // variavel tecla é igualada ao valor da tecla pressionada no IE	else if (e.which) // testa se é FF		var tecla = e.which; // variavel tecla é igualada ao valor da tecla pressionada no FF	if(tecla==13)		window.location = url+value;}/*Método JS para limpar o campo Perquisar quando o usuário clicar no campo na seção noticias*/$(function(){	  $('#busca_noticia').click(function(){	    $(this).val('');	  });	  	  });        function carrega_post(valor){	var ano_mes = valor.split("/")	var dsLoader = "<img src='"+IMG_SITE+"/global/loader.gif'>";    $('#posts').html(dsLoader);    $.post(URL_SITE+'blog/busca-post',{ano:ano_mes[0],mes:ano_mes[1] },function(data){        $('#posts').html(data);		      });		}