function buscar_ranking(url, value) {	window.location = url+value;}function buscar_ranking_bt(url, value, e) {	if (e.keyCode) // testa se é IE		var tecla = e.keyCode; // variavel tecla é igualada ao valor da tecla pressionada no IE	else if (e.which) // testa se é FF		var tecla = e.which; // variavel tecla é igualada ao valor da tecla pressionada no FF	if(tecla==13)		window.location = url+value;}/*Método JS para limpar o campo Perquisar quando o usuário clicar no campo na seção noticias*/$(function(){	$('#busca_ranking').click(function(){		$(this).val('');	});});